# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tool config (personal dev-server launch shortcut) — but .claude/skills/
# is real project tooling (e.g. the installer skill) and should be tracked.
.claude/launch.json

# Built by scripts/prepare-hook-bridge.sh for release bundles
src-tauri/binaries/
